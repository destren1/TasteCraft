import { TConstructorIngredient } from '@utils-types';

export type BurgerConstructorElementProps = {
  nonBunIngredientIndex: number;
  ingredient: TConstructorIngredient;
  index: number;
  totalItems: number;
};
